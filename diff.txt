OK...  want to go back to format ecology issues:  This was ChatGPT's suggestion:

I want to do a targeted diagnostic cleanup and tuning pass on the new format ecology / format economy diagnostic.

Do not run long simulations unless needed to validate syntax or obvious breakage. I mainly want the code and inspect page updated so I can run it myself.

Goals for this pass:

1. Fix the raw-format appendix counting logic
- Right now the raw appendix is suspicious because almost every format shows meanCount = 1.00.
- Audit the aggregation logic for the raw-format appendix in the format ecology diagnostic.
- Make meanCount truly market-wide across all sampled periods in the market/decade bucket, not conditional on “format exists in this row.”
- In other words:
  - if a format is absent in a sampled period, that should count as 0 for that period
  - meanCount should reflect average stations of that format per sampled period for that market+decade
- Keep meanSharePts as a market-wide per-period average too, not conditional on presence.

2. Add “share per surviving station” by bucket
- For each market+decade bucket and each commercial format bucket, add:
  - meanStationCount
  - meanSharePts
  - sharePerStationPts = meanSharePts / meanStationCount (guard against divide-by-zero)
- This should help reveal whether a bucket is strong because it has many surviving stations or because each station is economically strong.
- Include this in both:
  - the human-readable inspect HTML output
  - the structured JSON payload

3. Add a concentration diagnostic
- Add a new summary section for each market+decade:
  - rank commercial buckets by meanSharePts
  - compute:
    - top1SharePts
    - top2CombinedSharePts
    - top3CombinedSharePts
    - top1StationCount
    - top3BucketNames
- Also add a modern-only summary section (2010s + 2020s) showing these concentration values per market.
- Purpose: tell us whether New York is merely leaner, or actually too concentrated.

4. Specifically help diagnose spoken-word pileups
- Add a special breakout for the commercial spoken-word ecosystem:
  - ALL_NEWS
  - NEWS_TALK
  - SPORTS_TALK
  - PODCAST_TALK
- For each market+decade, report:
  - mean station count by raw format
  - mean share pts by raw format
  - remnant counts by raw format if available (weak/zombie/niche-survivor style logic)
- I want to see whether “news_talk bucket dominance” is really one strong lane or a pile of marginal spoken-word survivors.

5. Keep scope tight
- Do not change gameplay logic.
- Do not rebalance station economics yet.
- Do not modify attrition, audience curves, or public radio logic in this pass.
- This is a diagnostic instrumentation pass only.

Implementation requirements:
- Reuse the existing format ecology / format economy inspect page and harness structure.
- Keep naming consistent with the current diagnostic output.
- Preserve the current bucket mapping and existing sections unless they must be slightly adjusted to support the new metrics.
- If a metric is ambiguous, prefer explicit names over clever names.

Files:
- Update whatever inspect HTML / boot / harness / legacy helper files are needed.
- Keep changes surgical.

Deliverables:
- Briefly explain what you changed.
- Point me to the inspect HTML file to open.
- Do not spend time running the full report unless it is required to confirm the page builds.




